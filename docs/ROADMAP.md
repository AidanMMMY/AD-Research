# AD-Research — 功能路线图

**版本：** v1.0  
**日期：** 2026-06-01  
**状态：** 子项目1 已完成，子项目2 设计中  

---

## 一、项目概述

### 1.1 目标
将手工化的 ETF 投研工作流升级为**一体化 Web 投研平台**，支持全市场 ETF 的自动化跟踪分析、标的池管理、技术指标计算、策略回测和信号生成。

### 1.2 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│  子项目4：策略系统                                                │
│  ── 策略配置、回测引擎、信号生成、绩效归因                         │
├─────────────────────────────────────────────────────────────────┤
│  子项目3：Web 仪表盘                                              │
│  ── 前端界面、图表可视化、交互式分析                               │
├─────────────────────────────────────────────────────────────────┤
│  子项目2：ETF 投研核心（当前设计阶段）                             │
│  ── 全市场扫描、标的池增强、筛选排名、报告生成、综合评分             │
├─────────────────────────────────────────────────────────────────┤
│  子项目1：数据基础设施 ✅ 已完成                                   │
│  ── 数据采集管道、PostgreSQL 存储、ETL、指标计算、REST API          │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | FastAPI + SQLAlchemy 2.0 + Alembic |
| 数据库 | PostgreSQL 16 + Redis 7 |
| 数据源 | akshare（A股）+ yfinance（跨境） |
| 指标计算 | pandas + numpy |
| 定时任务 | APScheduler |
| 部署 | Docker Compose |
| 测试 | pytest |

---

## 二、子项目1：数据基础设施 ✅ 已完成

### 2.1 完成的功能清单

| 模块 | 功能 | 文件 |
|------|------|------|
| **项目框架** | Poetry 配置、Docker Compose、环境变量 | pyproject.toml, docker-compose.yml, Dockerfile |
| **配置管理** | Pydantic Settings、数据库/Redis 连接 | app/config.py, app/core/database.py, app/core/redis_client.py |
| **ORM 模型** | 11 张表（ETF、日线、指标、池、ETL日志、策略预留等） | app/models/etf.py, pool.py, etl.py |
| **数据库迁移** | Alembic 自动迁移 | alembic/versions/ |
| **数据源** | DataProvider 抽象、AkshareProvider、YFinanceProvider | app/data/providers/ |
| **数据管道** | 4层校验（格式/业务/时序/完整性）、ETL Pipeline 基类、A股日终采集 | app/data/transformers/, app/data/pipelines/ |
| **指标计算** | 技术指标（MA/RSI/MACD/ATR/布林带）+ 风险指标（波动率/回撤/夏普/区间收益）+ 批量计算引擎 | app/data/indicators/ |
| **API 接口** | 20+ REST 端点（ETF/池/行情/指标/分析/ETL管理） | app/api/v1/ |
| **定时任务** | 每天 15:30 采集 A股，08:00 计算指标 | app/core/scheduler.py |
| **数据初始化** | 全市场 1486 只 ETF 导入脚本 | app/scripts/init_etfs.py |

### 2.2 数据规模

| 数据 | 规模 |
|------|------|
| ETF 基础信息 | 1,486 只（A股场内） |
| 日线数据 | 按日采集，每只约 252 条/年 |
| 指标数据 | 每只每日 1 条 |
| 标的池 | 灵活配置，软删除设计 |

---

## 三、子项目2：ETF 投研核心（当前阶段）

### 3.1 功能优先级划分

基于**依赖关系**和**用户价值**，将 7 个功能分为 **P0（核心）** 和 **P1（进阶）**：

#### P0 核心功能（子项目2 实现）

这 4 个功能形成**"报告生产流水线"**：评分打分 → 筛选排序 → 池分析 → 生成报告

| 序号 | 功能 | 说明 | 依赖 |
|------|------|------|------|
| 1 | **综合评分系统** | 基于收益、风险、夏普、流动性等多维度给 ETF 打分 | 子项目1 的指标数据 |
| 2 | **全市场筛选排名** | 按评分/指标多条件筛选，生成 Top N 排名 | 评分系统 |
| 3 | **标的池增强** | 权重配置、持仓分析、池内对比、池间对比 | 子项目1 的池基础功能 |
| 4 | **报告生成引擎** | 定期自动生成 HTML/Markdown 研究报告，替代手工报告 | 评分 + 筛选 + 池分析 |

#### P1 进阶功能（子项目3 实现）

| 序号 | 功能 | 说明 | 依赖 |
|------|------|------|------|
| 5 | **板块/行业轮动分析** | 跟踪板块相对强弱、动量轮动信号 | 大量历史数据计算 |
| 6 | **全市场自动扫描** | 定期对比 akshare 列表和 DB，自动发现新增/退市/变更 ETF | 相对独立，可插入 ETL |
| 7 | **报告定时推送** | 邮件/企业微信等渠道自动推送生成的报告 | 报告引擎就位后 |

### 3.2 P0 功能详细说明

#### 3.2.1 综合评分系统

**目标：** 为每只 ETF 计算一个综合得分，便于横向对比。

**评分维度（建议）：**

| 维度 | 权重 | 指标 | 说明 |
|------|------|------|------|
| 收益能力 | 30% | return_1m, return_3m, return_1y | 近期和长期收益 |
| 风险控制 | 25% | volatility_20d, max_drawdown_1y | 波动和回撤 |
| 风险调整收益 | 25% | sharpe_1y | 夏普比率 |
| 流动性 | 10% | 日均成交额 | 避免流动性陷阱 |
| 趋势强度 | 10% | rsi14, ma5/ma20 位置 | 技术趋势 |

**输出：** 每只 ETF 的综合得分（0-100）和分项得分。

**存储：** `etf_indicators` 表新增 `composite_score`, `score_return`, `score_risk`, `score_sharpe`, `score_liquidity`, `score_trend` 字段。

#### 3.2.2 全市场筛选排名

**目标：** 基于多条件筛选 ETF，生成各类排名榜单。

**筛选条件：**
- 市场（A股/港股/美股）
- 分类（科技/周期/消费等）
- 指标范围（rsi_min/max, sharpe_min, volatility_max 等）
- 评分门槛（score_min）

**排名类型：**
- 综合评分 Top N
- 收益排名（1月/3月/1年）
- 风险最低排名（波动率/回撤）
- 夏普比率排名
- 趋势最强排名（RSI）

**输出：** JSON API + 嵌入报告。

#### 3.2.3 标的池增强

**目标：** 在子项目1 基础池功能上，增加投研所需的分析功能。

**新增功能：**

| 功能 | 说明 |
|------|------|
| **权重配置** | 池内每只 ETF 可配置目标权重（如 20%, 15%），实际权重偏离时提示 |
| **持仓分析** | 池的行业/板块分布、市值分布、QDII 占比等 |
| **池内对比** | 池内各 ETF 的收益/风险/相关性对比矩阵 |
| **池间对比** | 多个池的综合表现对比（收益曲线、回撤对比） |
| **再平衡提示** | 当实际权重偏离目标权重超过阈值时，提示需要再平衡 |

**数据模型变更：**
- `pool_members` 表新增 `target_weight`（目标权重）
- 新增 `pool_snapshots` 表（定期记录池的持仓快照，用于历史对比）

#### 3.2.4 报告生成引擎

**目标：** 替代用户目前手工生成的 HTML 报告，实现全自动定期生成。

**报告类型：**

| 报告 | 频率 | 内容 |
|------|------|------|
| **全市场扫描报告** | 每周 | 全市场 ETF 排名、新上榜/落榜、异常波动 |
| **标的池分析报告** | 每周 | 池内持仓分析、收益对比、风险提示 |
| **综合日报** | 每日 | 关键指标快照、持仓变化、市场概览 |

**报告内容模块（以周报告为例）：**

1. **市场概览** — 市场整体涨跌、成交额、情绪指标
2. **全市场排名** — 综合评分 Top 20、各行业最佳
3. **标的池分析** — 池内持仓表现、权重偏离、相关性矩阵
4. **重点关注** — 评分变化大的 ETF、新上榜/落榜、异常信号
5. **风险提示** — 高波动 ETF、低流动性警告、相关性过高的组合

**输出格式：**
- HTML（可浏览器直接打开，类似现有手工报告）
- Markdown（便于存档和版本管理）
- JSON（供前端仪表盘消费）

**报告存储：** 文件系统（reports/ 目录）+ 可选的 `reports` 表（记录报告元数据）

**定时触发：** 通过 APScheduler 在每周日晚上自动生成。

---

## 四、子项目3：Web 仪表盘（规划中）

### 4.1 目标
将报告和分析结果以可视化方式展示在 Web 界面中，支持交互式探索。

### 4.2 规划功能

| 功能 | 说明 |
|------|------|
| ETF 详情页 | 基础信息 + K线 + 指标趋势图 |
| 标的池管理页 | 创建/编辑池、拖拽调整权重、可视化持仓分布 |
| 全市场筛选器 | 多条件筛选 + 结果表格 + 排序 |
| 排名看板 | 各类 Top N 排名卡片 |
| 报告浏览页 | 历史报告列表 + 在线查看 |
| 相关性热力图 | 池内/自选 ETF 的相关性矩阵可视化 |
| 收益曲线对比 | 多只 ETF 的收益曲线叠加对比 |

### 4.3 技术选型（待定）

- 前端框架：待定（Vue/React/Svelte）
- 图表库：ECharts / TradingView / Plotly
- 与后端通过 REST API 通信（子项目1 的 API）

---

## 五、子项目4：策略系统（规划中）

### 5.1 目标
支持用户定义投资策略、回测验证、生成交易信号。

### 5.2 规划功能

| 功能 | 说明 |
|------|------|
| 策略配置 | 定义策略参数（如动量窗口、阈值等） |
| 回测引擎 | 在历史数据上运行策略，计算收益/回撤/夏普 |
| 信号生成 | 基于策略逻辑生成 BUY/SELL/HOLD 信号 |
| 绩效归因 | 分析策略收益来源（择时/选股） |
| 策略对比 | 多个策略的绩效对比 |

### 5.3 预留接口

子项目1 已预留表：
- `strategy_config` — 策略配置
- `backtest_results` — 回测结果
- `signals` — 交易信号

---

## 六、P1 进阶功能详细说明（供日后参考）

### 6.1 板块/行业轮动分析

**目标：** 识别当前市场热点板块和轮动趋势。

**分析方法：**
- 按 category 分组计算板块收益
- 计算板块相对强弱（RS）
- 识别动量领先的板块（过去 N 周收益排名）
- 板块轮动信号（领先板块切换时提示）

**输出：** 板块排名、轮动信号、热力图数据。

### 6.2 全市场自动扫描

**目标：** 自动发现新上市/退市/变更的 ETF，保持数据库与 akshare 同步。

**扫描逻辑：**
1. 从 akshare 获取最新 ETF 列表
2. 与 `etf_info` 表对比
3. 新增：插入新记录，status="active"
4. 退市：更新 status="delisted"
5. 变更（名称/分类等）：更新字段

**触发：** 每周日凌晨 03:00（通过 APScheduler）。

### 6.3 报告定时推送

**目标：** 将生成的报告自动推送到指定渠道。

**推送渠道：**
- 邮件（SMTP）
- 企业微信（Webhook）
- 飞书（Webhook）
- 钉钉（Webhook）

**配置：** `data_source_cfg` 表扩展或新增 `notification_config` 表。

---

## 七、数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                        外部数据源                                │
│              akshare (A股)    yfinance (跨境)                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  子项目1：数据基础设施 ✅                                         │
│  ── 数据采集 → 清洗/校验 → 存储 → 指标计算 → REST API              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        ┌─────────────────────┼─────────────────────┐
        ↓                     ↓                     ↓
┌───────────────┐   ┌─────────────────┐   ┌─────────────────┐
│  综合评分系统  │   │  标的池增强      │   │  全市场筛选排名  │
│  (子项目2)     │   │  (子项目2)       │   │  (子项目2)       │
└───────┬───────┘   └────────┬────────┘   └────────┬────────┘
        │                    │                     │
        └────────────────────┼─────────────────────┘
                             ↓
                    ┌─────────────────┐
                    │  报告生成引擎    │
                    │  (子项目2)       │
                    └────────┬────────┘
                             ↓
        ┌────────────────────┼─────────────────────┐
        ↓                    ↓                     ↓
┌───────────────┐   ┌─────────────────┐   ┌─────────────────┐
│  HTML/Markdown │   │  Web 仪表盘      │   │  定时推送        │
│  报告文件      │   │  (子项目3)       │   │  (子项目3)       │
└───────────────┘   └─────────────────┘   └─────────────────┘
```

---

## 八、附录

### 8.1 已有 API 清单（子项目1）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/api/v1/etfs` | GET | ETF 列表（分页/筛选/搜索） |
| `/api/v1/etfs/{code}` | GET | ETF 详情 |
| `/api/v1/etfs/categories/list` | GET | 分类列表 |
| `/api/v1/etfs/markets/list` | GET | 市场列表 |
| `/api/v1/pools` | GET/POST | 标的池列表/创建 |
| `/api/v1/pools/{id}` | GET/PUT/DELETE | 标的池详情/更新/删除 |
| `/api/v1/pools/{id}/members` | POST | 添加 ETF 到池 |
| `/api/v1/pools/{id}/members/{code}` | DELETE | 从池移除 ETF |
| `/api/v1/market-data/{code}/history` | GET | 历史日线 |
| `/api/v1/market-data/snapshot` | GET | 最新行情快照 |
| `/api/v1/indicators/{code}` | GET | 最新指标 |
| `/api/v1/indicators/{code}/history` | GET | 指标历史 |
| `/api/v1/indicators/batch` | GET | 批量指标查询 |
| `/api/v1/analysis/correlation` | GET | 相关性矩阵 |
| `/api/v1/analysis/ranking` | GET | ETF 排名 |
| `/api/v1/analysis/screen` | GET | 条件筛选 |
| `/api/v1/etl/status` | GET | ETL 状态 |
| `/api/v1/etl/logs` | GET | ETL 日志 |

### 8.2 项目目录结构

```
ad-research/
├── app/
│   ├── main.py                 # FastAPI 入口
│   ├── config.py               # 配置管理
│   ├── core/                   # 核心工具
│   │   ├── database.py         # SQLAlchemy
│   │   ├── redis_client.py     # Redis
│   │   ├── scheduler.py        # APScheduler
│   │   └── exceptions.py       # 异常
│   ├── models/                 # ORM 模型
│   │   ├── etf.py              # ETF/日线/指标/汇率
│   │   ├── pool.py             # 池/成员
│   │   └── etl.py              # ETL/策略预留
│   ├── schemas/                # Pydantic 序列化
│   ├── services/               # 业务逻辑
│   ├── api/                    # REST API
│   │   ├── deps.py             # 依赖注入
│   │   └── v1/                 # v1 路由
│   ├── data/                   # 数据管道
│   │   ├── providers/          # 数据源
│   │   ├── pipelines/          # ETL 管道
│   │   ├── transformers/       # 清洗/校验
│   │   └── indicators/         # 指标计算
│   ├── tests/                  # 测试
│   └── scripts/                # 运维脚本
├── alembic/                    # 数据库迁移
├── docker-compose.yml          # Docker 编排
├── Dockerfile                  # 后端镜像
├── pyproject.toml              # Poetry 配置
└── docs/                       # 文档
    └── ROADMAP.md              # 本文件
```

### 8.3 变更日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-06-01 | v1.0 | 初始版本，子项目1 完成，子项目2-4 规划 |
