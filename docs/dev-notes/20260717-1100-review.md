# 2026-07-17 11:00 复盘报告（草稿，待 10:45 最终更新）

> 生成时间：2026-07-17 07:20 CST
> 本报告将在 10:45 根据最新进度更新最终数字。

## 一、当前整体状态

- **ECS 服务**：backend / nginx / postgres / redis 均 healthy；celery-worker 已恢复运行。
- **A 股指标补齐**：`etf_indicator.trade_date = 2026-07-15` 当前 **1,041** 条，目标约 **7,000+** 条。
  - 已拆分 ETF / STOCK 两个 shard 并行处理；额外追加了一个 STOCK shard。
  - 当前 4 个 worker 在跑：ETF、STOCK×2、一个遗留的 07-17 任务。
  - 预计完成时间：**09:30–10:00**（视 DB 负载与并发效率）。
- **队列状态**：
  - `indicator`：0（任务已被 worker 领走）
  - `cninfo`：17 个任务暂停（worker 暂只监听 indicator）
  - `celery`：0（已清理旧任务）
- **磁盘空间**：`/data` 已从 90% 降至 **83%**，并新增每周日凌晨 3 点自动清理 cron。

## 二、本次会话已完成的工作

| 类别 | 内容 |
|------|------|
| 部署/队列 | 部署 `5f5f07b`；配置 Celery `task_routes`；worker 监听 indicator 队列；清理默认队列中的旧 indicator 任务 |
| 指标补齐 | 触发 ETF+STOCK 07-15 任务；追加 STOCK shard；启动后台进度监控 |
| 磁盘清理 | 删除旧镜像 `ad-research:1e85e9f`；新增 `/root/docker-cleanup.sh` + 每周日 cron |
| 故障清理 | 重启 backend 清理僵尸 fast_catchup 进程；kill 掉 10+ 个卡死的递归 CTE 查询 |
| cron 维护 | 移除失效的 `/opt/ad-research/...` 每分钟 cron |

## 三、仍然未解决的问题

1. **A 股 07-15 指标未补齐**：仍需约 6,000 条，预计 2.5 小时内完成。
2. **celery-worker 尚未恢复三队列监听**：indicator 完成后必须改回 `-Q celery,indicator,cninfo`。
3. **cninfo 队列 17 个任务仍暂停**：待 worker 恢复三队列后消费。
4. **SQL indicator 查询仍有优化空间**：单 chunk 20 codes 约 30–40s；可考虑调大 chunk size 或减少 `max_bars`。
5. **SSH 到 ECS 频繁 reset**：本会话中多次遇到，影响操作效率，需排查网络/安全组/主机负载。
6. **/data 40G 容量偏紧张**：长期需扩容或归档历史 cninfo PDF / Postgres WAL。

## 四、处理失败 / 做得不够好的地方

1. **fast_catchup 多进程脚本险些拖垮 DB**：启动 4 进程后产生大量并发递归 CTE，连接异常、Postgres 后端崩溃，最终不得不全部 kill 掉。
2. **未能第一时间识别 celery 默认队列中的旧任务**：清理后才让 worker 专注跑 07-15。
3. **SSH 操作因引号转义多次失败**：复杂命令在 ssh -c 中容易出错，应优先用本地脚本 + scp。
4. **对 SQL backend 的并发承载能力估计不足**：4 个并发查询是稳定上限，不应再叠加额外进程。
5. **没有及时检查 `pg_stat_activity` 定位查询来源**：一开始误以为是 Celery worker，后来发现是 fast_catchup 僵尸进程 + 连接池重连。

## 五、关键决策与临时状态

- **worker 临时仅监听 indicator**：为在 11 点前完成 07-15 补齐，当前合理；完成后必须恢复。
- **STOCK shard 重复执行**：追加了一个 STOCK shard，两个 STOCK 任务会处理相同代码，依赖 `ON CONFLICT DO UPDATE` 去重；存在轻微锁竞争，但可接受。
- **没有 push 新代码**：按记忆约束，未 push 到 GitHub；所有变更均为 ECS 本地操作或监控脚本。

## 六、后续必须执行的操作

1. 10:45 检查 `etf_indicator` 07-15 行数；若接近 7,000 且 indicator 队列为空：
   ```bash
   ssh -i ~/.ssh/claude_aliyun root@47.239.13.111
   sed -i 's/-Q indicator/-Q celery,indicator,cninfo/' /data/ad-research/deploy/aliyun-ecs/docker-compose.yml
   cd /data/ad-research/deploy/aliyun-ecs
   docker compose up -d celery-worker
   ```
2. 清理 ECS 临时脚本：`/tmp/fast_catchup_0715.py`, `/tmp/fast_catchup.log`, `/tmp/monitor_indicator.sh`, `/tmp/indicator_progress.log`。
3. 评估是否扩大 ECS `/data` 磁盘或迁移旧 cninfo PDF。
4. 安排时间优化 indicator SQL chunk size / max_bars。

## 七、代码提交总结

本次会话无新 GitHub push。已部署的 commit：

- `9ac6dd9` feat(celery): 队列路由
- `e1fc3eb` ops(compose): 默认 INDICATOR_BACKEND=sql
- `9546904` fix(docker): entrypoint chown CNINFO_PDF_DIR
- `1e85e9f` perf(sql-indicator): 限制最近 500 bars
- `5f5f07b` fix(sql-indicator): windowed CTE 补 amount/volume

---

*最终数据将在 10:45 更新。*
